shoot1~shoot4 为四个射字母程序，其编译结果可分别在 myOS.img 的 2~5 扇区查看。

init_monitor 为实验内容第2点（无输入命令的监控程序），其监控程序为 init_monitor.asm，虚拟机为 init_monitor.bxrc，软驱为 init_monitor.img。

myOS 为最终成品，其监控程序为 monitor.asm，虚拟机为 myOS.bxrc，软驱为 myOS.img。

！！！数字键请使用字母上方的横排，勿用小键盘！！！